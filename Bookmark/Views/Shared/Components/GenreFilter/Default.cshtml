@model IEnumerable<Bookmark.Models.DomainModels.Genre>

@foreach (Bookmark.Models.DomainModels.Genre g in Model)
{
    <my-link-button action="Index" id="@g.GenreId"
                    class="me-1 mb-2">@g.Name</my-link-button>
                }


